@model DataLayer.Models.User.LoginViewModel

@{
    ViewData["Title"] = "Login";
    Layout = "LoginLayOut";
}

@{
    ViewData["Title"] = "ورود";
}
<div class="mt-3 container col-6 rounded-2 border border-2 bg-light">

    <h2>ورود</h2>
    
<pre>

</pre>
    <form method="post">

        <div class="row mb-3">
            <label for="inputEmail3" class="col-sm-2 col-form-label">نام کاربری:</label>
            <div class="col-sm-10">
                <input asp-for="Name" class="form-control" id="inputEmail3">
            </div>
        </div>



        <div class="row mb-3">
            <label for="inputPassword3" class="col-sm-2 col-form-label">کلمه عبور:</label>
            <div class="col-sm-10">
                <input asp-for="Password" type="password" class="form-control" id="inputPassword3">
            </div>
        </div>

        <div>
            <label><input asp-for="RememberMe" type="checkbox" /> مرا به خاطر بسپار</label>
        </div>

        <div asp-validation-summary="All" class="text-danger"></div>
        <pre>

    </pre>
        <button type="submit" class="btn btn-success">ورود</button>
        <a style="margin-right:20px;" class="btn btn-primary" asp-action="ForgetPassword">کلمه عبور را فراموش کرده ام</a>
    </form>
<pre></pre>

</div>
